# Changelog

## 1.9.0 — 2026-09-23

**摄影机语言拆成四层：身份、支撑、运动、质感；给"资料说适合、但 Seedance 上有风险"的组合建一张风险表；更正希区柯克变焦的方向。** 这是导演核心的有意变更，保护区哈希在本提交重设。**工程完成：测试通过（145 项），已提交、打 tag v1.9.0、推送。效果未验证**：运动质感能否被模型区分已列入待验证 15、16。

### 触发与评估
用户提供资料《摄影机运动与支撑语言》（未注出处），要求评估它的价值与准确性，并接入 skill。用户选方案 A，并要求再核对"资料说适合、但 Seedance 上有风险"这一类。

评估：
- **重复的部分（约四成）**：动机原则、推 / 拉 / 摇 / 跟等运动的叙事职能，skill 已有，而且带一手出处（硬规则 8、§2.1–2.2、S5 §5.4）。
- **新的部分**：摄影机身份、支撑与运动分开、运动质感、克制手持。
- **事实错误**：资料写"摄影机后移同时变焦推近 → 背景空间被拉伸"，方向反了。维基百科 Dolly zoom 条目示意图注是"the teapots in the background grow bigger … camera moving back while zooming in"。skill 自己的 `director-grammar.md` 也写成了"背景被拉远"，`camera-vocabulary.md` 的示例同样有误。
- **过度概括**："现代剧情片很少用变焦"（反例：Kubrick《巴里·林登》、洪常秀）；情绪对应和"很适合"都是经验说法，统一标 `[第三方·未注出处]`。
- **风险核对**：逐条对照资料的"很适合"清单，与能力表 §4.5–§5、验证日志，以及 THE ORDER EP03 场 4 clip01–03 的抽帧审片（项目 `08_renders/README.md`）。归成七类：
  - ① 人群：设计十几人，出片只有四人；
  - ② 快速运动；
  - ③ 说台词时的运动：侧脸口型不可靠 `[第三方]`；正面反向跟移配对白执行到位；侧跟只验证过短句；
  - ④ 长时间、复合、纵深运动；
  - ⑤ 质感能否被分辨：未验证，抽帧看不出质感；
  - ⑥ 揭示新空间：clip01 是海，clip03 是石头大楼；
  - ⑦ 主观镜头露出手。

### Changed
- `references/director-grammar.md`：
  - §2.2 希区柯克变焦行改为"机器后拉同时变焦推近，人物大小不变、背景变大压向人物"（前推 + 拉焦则背景变小退远），附出处；
  - §2.3 与 §2.4 恢复正常顺序（1.8.0 时插反了），内容不变；
  - **新增 §2.5**，内容包括：
    - 四层表，以及决策顺序：身份 → 动机成立时选运动 → 支撑与质感；不按场景类型选；
    - 同一表演配不同运动的四个例子；
    - 摄影机身份 9 种，只是候选、不是映射，并写明与"视点"的区别；
    - 支撑 11 种：每种写质感、资料说适合什么、对应风险编号、Prompt 写法；官方术语里没有的器材写"非官方术语"加可见描述；
    - 运动质感 6 种，由资料的 10 个标签压缩而来；
    - 风险表 ①–⑦：每行写情形、资料举的例子、依据标签、处理办法；风险意味着换载体、写具体或登记，不是禁用。
- `references/stage-5-directing-storyboard.md`（保护区）：
  - §5.1b "本场运动逻辑"一行先写摄影机身份：默认整场一个，单镜换身份写理由；
  - §5.4 动机表加 3 行（靠近人物 → 克制手持；跟人物进入新空间 → 身后跟拍或斯坦尼康；强化两人关系 → 双人镜头加缓推，无台词段才可弧移），并加一句"先过风险表 ①–⑦"。
- `templates/shot-card.md`（保护区）：头部加"摄影机身份"；C 层加可选的"支撑 / 质感"。
- `references/stage-6-prompt-compiler.md`（保护区）：§6.2 加"支撑与质感写成看得见的结果；器材名只有官方术语里有的才直接写；摄影机身份不进 Prompt"。
- `references/stage-7-qa-continuity.md`（保护区）：§7.1 加"摄影机身份与支撑"审阅行；§7.2 加"运动质感不对"一行，处理方式是改写成可见结果并记验证日志，不建议抽卡。
- `references/camera-vocabulary.md`（保护区）：更正希区柯克变焦示例；运镜库说明段加一句，指向 §2.5。
- `references/camera-index.json`：46 条都加上 `layer`（运动 / 支撑 / 特效；手持、Snorricam、载具跟归为支撑）和 `support`（默认支撑）。`scripts/camera_library.py`（保护区）的 `--slots` 多印这两项。
- `SKILL.md`：硬规则 8 末尾加一句（身份按场定，支撑与质感写成可见结果）；快速路由加一行。
- `references/validation-log.md`：表中加 THE ORDER EP03 场 4 clip01–03 的运镜执行记录；待验证队列加第 15 条（运动质感能否被区分）和第 16 条（侧跟或弧移配 ≥8 词台词时的口型）。
- 除希区柯克变焦那一处有意更正外，所有被改的已有行都只在行内追加，逐行比对原文 100% 保留。变化轨、W24 / W30–W34、情绪库、运镜库五段填写均未改动。

### Tests
- `tests/test_camera_language.py`（7 项）：
  - 两份文档里希区柯克变焦的方向都已更正（已确认这条测试在旧文本上会报错）；
  - §2.1–§2.5 顺序正确，§2.1 的 Deakins 原文仍在；
  - 四层、9 种身份、11 种支撑（非官方术语的器材都写了描述）、6 种质感；
  - 风险表七行都有依据标签和处理办法，并包含人群实测与"弧移不用于两人对白"；
  - S5 / 分镜卡 / S6 / S7 / SKILL / 词表的接线，以及原有审阅行和动机表行仍在；
  - 验证日志的新记录；
  - 索引的 `layer` / `support` 字段。
- `tests/test_protected_zone.py`：重设 6 个文件的哈希。

**已知未处理**：
- §2.5 的身份、质感说法没有一手出处，按用户资料录入，已标注。
- 风险表中人群、新空间、口型三条各只有 THE ORDER 一个样本。
- 质感能否被区分完全未验证。
- 没有新增 W 校验：身份与质感属于导演决策，脚本判断不了对错，放在 S7 人工审阅。

## 1.8.0 — 2026-09-23

**接入运镜库：先定动机，再按五段填成这一镜的内容，不照抄。** 导演核心的有意变更，保护区哈希在本提交重设，新增三个受保护文件。**工程完成：测试通过（138 项），已提交、打 tag v1.8.0、推送。效果未验证**：没有做过"填写 vs 原文"的成片对照。

### 触发与设计
用户提供《AI 运镜提示词库》（`~/Desktop/AI运镜提示词库/`，46 条、7 类、英文），结构与情绪库类似。用户要求：用在导演逻辑里，落到导演方案和 Prompt 编译；不照搬原文，不做成机械套用。

核查：
- 库里每条拆成五段 Camera / Movement / Speed / Framing / End。
- 它补了 skill 缺的一块："运动中什么保持不变"（Framing）。
- 它没有动机，也没有"什么时候不用"和风险。
- 它的 End 全是通用句。
- 项目里现成的反例：THE ORDER EP03 场 4 clip01 镜 1（用户指定）把 shot-19 整段英文原样贴进中文 Prompt。结果 "the subject" 在整队人里指谁不清楚，本镜真正的终点没进运镜，还中英混写。

用户选方案 A（索引 + 五段填写），并定了三点：
- 库复制进 skill 并锁定哈希；
- 英文五段结构只在英文 Prompt 或用户指定原文时保留；
- 默认按五段填写。

### Added
- `assets/camera-library.json`：`all_prompts.json` 的原样副本（SHA-256 `0f3136a5…88ec`，与源文件一致），进入保护区。
- `references/camera-index.json`：46 条中文阅读索引。每条写：
  - 中文名、检索词；
  - 叙事职能：指回 `director-grammar.md` §2.2 等处的行，库外新增的职能标 `[推论]`；
  - 什么时候不用、人物运动关系；
  - 载体与风险：大角度环绕、精确前景交给关键帧或白模，特效类标 `[未验证]`；
  - 能力表 §4.5 官方术语里的写法；
  - `camera-vocabulary.md` 里的对应行。
  
  这份索引不锁定，以便修订。
- `references/camera-library.md`：用法文档，内容包括：
  - 决策顺序：观众需要什么 → 要不要动 → 哪类运动 → 选条目 → 五段填写；
  - 五段落点：Camera / End 进标注，Movement / Speed / Framing 进运镜展开句，动机留在 A 层；
  - EP03 例子的照抄版与填写版对照；
  - 场级"本场运动逻辑"；
  - 三种来源：填写（默认）、原文（仅用户指定）、库外；
  - 检查项与查不到的部分；效果状态。
- `scripts/camera_library.py`（保护区）：`--list` / `--id` / `--query` / `--slots`（五段填写模板，附索引说明）/ `--raw`。
- `scripts/camera_checks.py`（保护区），由 `validate_prompt.py` 调用，输出 JSON `camera`：
  - **W34**：某镜照抄库里 Movement / Speed / Framing / End 的英文通用句（≥5 词；更短的 "still and steady." 这类会在自然英文里出现，不查），或中文 Prompt 里夹着英文五段骨架，且这一镜没声明原文。
  - **W35**：E 层 `运镜来源` 一行不成立：条目编号不存在、镜号不存在、原文没写编号、方式读不出。
  - 查不到的：逐字译成中文的通用句。

### Changed
- `SKILL.md`：
  - 硬规则 8 末尾加一句：运镜库只在动机定下后提供五段写法；
  - 流水线 S5 行加 `camera-library.md`；
  - 快速路由加"用运镜库 / 这个运镜怎么写"。
- `references/director-grammar.md`：新增 §2.4 "运镜库：执行语法"。§2.1–2.3 原文不动。
- `references/stage-5-directing-storyboard.md`（保护区）：§5.1b 视觉方案加"本场运动逻辑"一行；§5.4 在动机表之后加运镜库一段（取条目、五段填写、分镜卡写法、运镜来源、库不决定要不要动）。
- `references/stage-6-prompt-compiler.md`（保护区）：§6.2 加五段落点规则；§6.5 E 表加 `运镜来源`；§6.6 自检加一问。
- `references/stage-7-qa-continuity.md`（保护区）：§7.1 审阅表加"运镜库"一行。
- `templates/shot-card.md`（保护区）：C 层加可选的"运镜库条目"五段行；E 元数据加 `运镜来源`。`templates/prompt-templates.md`（保护区）：E 层字段说明加 `运镜来源`。
- `references/camera-vocabulary.md`（保护区）：运镜一节加一行，指向库与索引。
- 所有被改的已有行都是只在行内追加：逐行比对，原文字符 100% 保留，没有删除任何条款。变化轨、W24、W30–W33、情绪库均未改动。

### Tests
- `tests/test_camera_library.py`（12 项）：
  - 副本 SHA 等于源文件，共 46 条、7 类；
  - 索引覆盖全部条目、字段都有值、§ 引用能在 `director-grammar.md` 里找到、库外职能标推论；
  - `--slots` / `--raw` / `--query`；
  - 短句不进照抄检测；
  - EP03 真实 Prompt（`tests/fixtures/camera/`，原样复制）报 W34；声明原文后通过并出 INFO；填写版通过；
  - 中文 Prompt 夹英文骨架报 W34；五种不成立的运镜来源都报 W35；运镜来源行解析；英文 Prompt 写本镜内容不报；
  - 文档接线：动机表在运镜库段之前、§2.1 保留。
- `tests/test_protected_zone.py`：新增三个受保护文件；重设 7 个文件的哈希。
- 回扫：THE ORDER 与 reckless 已出的 48 条 Prompt，只有 EP03 场 4 clip01 镜 1 报 W34，无误报。

**已知未处理**：
- 库条目在 Seedance 2.5 上的执行整体未验证。
- W34 只认英文原句。
- 动机成立与否、终点是不是这一镜的构图，属语义审阅。
- 项目里 EP03 场 4 clip01 那一镜，重新跑校验会报 W34。按规则应在 E 层补 `运镜来源 | 镜1 shot-19 原文`；本次没有改项目文件。

## 1.7.0 — 2026-09-23

**英文语速默认值从 3.5 改为 4 词/s。** 用户定："默认改成 4"。1.6.1 按我对上一条指示的误读统一成了 3.5，这里更正。这次会改变校验行为：没写 `语速词每秒` 的 Prompt，W05 / W22 / W26 / W29 都改按 4 词/s 估时。保护区文件哈希在本提交重设。**工程完成：测试通过（126 项），已提交、打 tag v1.7.0、推送。效果未验证**：没有用 4 词/s 的默认值做过生成对照。

### 依据
- 用户判定 `[用户判定]`。
- 实测 D10b 整段 ≥4.1 词/秒仍出片可用 `[实测]`（`validation-log.md`）。
- THE ORDER 的分镜本来就按用户定的 4 词/秒排。
- 2.5 仍是美式英语均值，也是下限；慢戏照旧显式写 2.5–3，并写「语速理由」。

### Changed
- `scripts/validate_prompt.py`（保护区）：`speech_parameters` 的英文默认值 3.5 → 4.0。估时 INFO 行变成"台词估算 4 字/s，4 词/s，…"。
- `SKILL.md` 硬规则 17：默认 4 词/s，短促可到 4.5。
- `references/seedance-2.5-capabilities.md`（保护区）：
  - §7.2 默认 4；短促档改为 4.5 词/s，标 `[推论]`，理由是它落在实测整段 ≥4.1、单句约 4.7 的可用范围内；
  - §7.3 预算表的三列改为 慢 2.5 / 默认 4 / 短促 4.5，按"窗口 × 语速 × 2/3"重算取整。"Now turn…"那句（9 词 / 3 s）按 4 词/s 估 2.25 s。
- `references/duration-rhythm.md` §二 第 1、8 条，以及标题行。
- `references/stage-4-performance.md` §4.5、`references/stage-6-prompt-compiler.md` 节奏指令与 E 表、`templates/prompt-templates.md` E 层说明与示例（均为保护区）；`references/scene-parameters.md`。
- `references/validation-log.md` 标签变更登记加一行（3.5 → 4）。

### Tests
- `test_performance`：`speech_parameters` 的默认值改为 4.0，并新增一条"显式写 3.5 仍按 3.5"。
- `test_production`：两处估时改为按 4 算。
- `test_variation`：
  - 摄影师那句按 4 词/s 仍放得下，也不报 W22（建议窗口 2.5 s，实际 3 s，松弛 0.5 s）；
  - 文档一致性测试扩到 7 个文件：不能再有任何文档把 2.5 或 3.5 写成默认值，每个文件都要写明默认 4。
- `test_protected_zone`：重设 validate_prompt.py、capabilities、stage-4、stage-6、prompt-templates 五个文件的哈希。

## 1.6.1 — 2026-09-23

**S4 的英文语速默认值与硬规则统一为 3.5 词/s。** 只改文档，不改行为：`validate_prompt.py` 自 1.4.0 起就按 3.5 估时。**工程完成：测试通过（126 项），已提交、打 tag v1.6.1、推送。**

### 触发
检查 25 条情绪提示词在 Prompt 编译中怎么用时发现：`references/stage-4-performance.md` §4.5 仍写"英文默认 2.5 词/s"。1.4.0 已把硬规则 17、能力表 §7.2 和校验脚本改为 3.5（2.5 是美式英语均值、是下限），但 stage-4 在保护区里，当时漏改了。用户定："修复，并改成默认 3.5"。

### Fixed
- `references/stage-4-performance.md` §4.5（保护区，哈希在本提交重设）：改为"英文默认 3.5 词/s（常速交锋；短促可到 4；慢戏 2.5–3 须写明说法，2.5 是美式英语均值、是下限不是默认）"，其余不动。
- `references/scene-parameters.md` §二"模型执行"：同样过时的"默认估算 4 字/s、2.5 词/s""语速仍为 4 字/s、2.5 词/s"改为 3.5。

### Added
- `tests/test_variation.py`：SKILL.md、stage-4、scene-parameters、能力表四处都不能出现"默认…2.5 词"，且都写 3.5。已确认这条测试在旧文本上会报错。
- `tests/test_protected_zone.py`：重设 stage-4 哈希，docstring 记 1.6.1。

未改：`validation-log.md` 待验证队列第 10 条"按 2.5 词/s × 2/3 占比预算时口型是否完整"是历史待验证项，保留原文。

## 1.6.0 — 2026-09-23

**每条 clip 至少交付一次变化（硬规则 20）：场级方案的 clip 切分表加"交付变化"列。** 属于导演核心的有意变更：S5、S7 两个保护区文件只增不删，哈希在本提交重设。**工程完成：测试通过（125 项），已在本地提交。效果提升未验证**：只在一个场景上回填过（非盲），还没有用新流程排过一场分镜。

### 触发
THE ORDER EP03 场 4 v4.1 新增的"几分钟后，牛棚边"是一段没有事件的无台词段。film-creative 3.7 的事件轨回填后，这一行是余韵：Isa 场末仍是"推迟"。S5 把它排成了 s04-clip04：四镜 16 s，整条 clip 只有这一段。用户看成片问："这 16 s 存在的意义是？"

核查本 skill 这一侧：
- clip04 的卡上**峰值事件**（Diego 拍手套）、**观众问题**（他会走进去吗）、**无声段理由**（本集最后一个画面，回答推迟到 EP05）都填了。W24、W30–W33 都通过。1.4.0 的变化轨要求每条 clip 声明一个峰值，于是从没有变化的段落里造出了一个峰值。
- 1.5.0 的场级比值也没有指向它：clip04 的 16 s 和剧本给这一行的估时 15 s 几乎一样，整场 1.28 的超出落在跑道段。
- 结论：S5 切 clip 时只问"时长和画面怎么排"，没有一步问"这条 clip 交付了什么变化"。用一句话写理由的字段总能被填满；能拦住空段的，只有指向剧本某一行、脚本查得出真假的引用。

用户在 A（方案加两列 + 无台词启发式判定）、B（A + 分镜卡字段，保护区发布）、C（每条 clip 写交付变化，对全场变化行做划分）中选 **C**。实现中用户提醒：不要做成"为一个模板开发"，不要丢掉原有的画面变化、review 能力。据此作了两处调整：
- 没有事件轨的剧本改为由分镜自己写"谁：进 → 出"，不再只查非空。规则不依赖 film-creative 的模板，模板只让它能按行号核对。
- 所有措辞写明：这一列管"这条 clip 该不该存在"，变化轨与无声段功能管"clip 里怎么拍"，两边照常都查，互不代替。

### Added
- `scripts/scene_track.py --plan` 的交付变化检查。场级方案里首列为 clip、带"交付变化"列的表，每条 clip 写行号，例如"第 3–6 行""第 1、2 行"。括号里和叙述里的数字不算。
  - **剧本有事件轨时，以下报问题**（退出码 1）：
    - 方案的 clip 表缺这一列，或找不到 clip 表；
    - 某条 clip 没有交付任何变化行（只有余韵、写"无"或空）。问题文字给两种改法：并进前一次变化所在的 clip 尾部；或删掉这条 clip，在差异行写"第 N 行不拍"并提醒用户；
    - 同一变化行由两条 clip 交付；
    - 行号不在事件轨里。
  - 余韵行可以写进交付变化列，但不计入交付。
  - 没有 clip 交付的变化行只提醒，不算问题。
  - 输出逐条 clip 列出交付那几行的"变化"与"删掉损失"。
  - **没有事件轨时**：这一列必须含"→"、不以"无"开头。
  - 读事件轨时新增"变化""删掉损失"两列。3.6 场面轨的"主要活动（类别；变化写括号）"也含"变化"二字，所以这两列只在事件轨里读。JSON 输出加 `delivery`、`undelivered`、`delivery_reminder`。
- `tests/fixtures/scene-track/theorder-ep03-s04-plan-delivery.md`：项目方案快照 + 维护者回填的"交付变化"列（非盲）。回填按各 clip 的分拍与事件轨各行锚句的归属对齐：clip01 第 1–2 行、clip02 第 3–6 行、clip03 第 7–10 行（第 10 行是余韵尾巴）、clip04 第 11 行（只有余韵）。
- `tests/test_scene_track.py` +9：
  - clip04 报"没有交付任何变化行"，clip01–03 通过；
  - 删掉 clip04 并写差异行、或把第 11 行并进 clip03，都通过；
  - 同一变化行交付两次报问题；
  - "无""—""第 12 行"都报问题；
  - 未交付的变化行只提醒；
  - 快照方案缺列时，新旧两种格式都报问题；
  - 没有事件轨时，"谁：进 → 出"放行，"无"、叙述句、行号都报问题；
  - `parse_refs` 行号解析；
  - JSON 输出；
  - 文档接线测试，同时断言硬规则 19、§5.1f 峰值条款原文、S7 原有审阅行、`duration-rhythm.md` 变化轨仍在。
- 旧测试两处改动：
  - 1.5.1 的"加差异行后无问题"改为"加差异行后没有差异行问题"，因为快照方案现在另报缺交付变化列；
  - 1.5.0 的"场面轨 + 差异行通过"改用按新规则写"进 → 出"的方案。

### Changed
- `SKILL.md`：新增**硬规则 20**；输入交接、流水线 S5 行各补一句；快速路由加"这 N 秒存在的意义是？"一行（设计问题，不抽卡、不另造峰值）。
- `references/stage-5-directing-storyboard.md`（保护区）：
  - §5.1 新增第 6 条：交付变化列；余韵不单独成 clip；导演加的镜头放进交付了变化的 clip；不依赖模板；
  - §5.1f 第 1 条句尾加一句：峰值事件从交付的变化行里选，原文未改；
  - §5.9 加一行。
- `references/stage-7-qa-continuity.md`（保护区）：§7.1 审阅表加"交付变化"一行，§7.2 归类表加"这 N 秒存在的意义是？"一行。
- `references/duration-rhythm.md` §十一 新增第 6 条：比值管不到哪一条 clip 空；检查项、提醒、旧稿写法；这一列不取代变化轨。
- `tests/test_protected_zone.py`：重设 stage-5、stage-7 的哈希，docstring 记 1.6.0。README 版本与简介。

**已知未处理**：
- 交付变化由分镜自填：写得出一个行号或一句"进 → 出"，脚本就放行。变化在成片里看不看得见，归 S7 审片与 `measure_clip.py`。
- 检查只核对"哪条 clip 交付哪行"，不核对 clip 的实际内容是否对应那几行；也管不到余韵在 clip 内部被拉长（比如 2 s 的余韵拖成 8 s）。
- 分镜卡本身不带这一列（方案 B 未做）。
- 校准样本只有 EP03 场 4 一个场景，换项目要重看。

## 1.5.1 — 2026-09-23

**`scene_track.py` 读 film-creative 3.7.0 的"## 事件轨"。** 保护区未动（改动的脚本、模板、文档都不在哈希表内）。**工程完成：测试通过（115 项），已提交并打 tag v1.5.1。** 只是格式兼容，不涉及效果判断。

### 触发
film-creative 3.7.0（tag v3.7.0）把 3.5 的"## 本场赌注"卡与 3.6 的"## 场面轨"合成一张"## 事件轨"：人物清单 + 一行一次变化（# / 谁 → 对谁 / 变化：进 → 出（类别）/ 说出口 / 删掉损失 / 地点（子空间）/ 活动 / 时间 / 锚句 / 估时），没有变化的行写"无（余韵：…）"。本 skill 的 `TRACK_HEAD` 只认"## 场面轨"，新剧本页会回退成"没有场面轨"：场地清单为空、剧本估时取不到、`--plan` 不再要求差异行。

### Changed
- `scripts/scene_track.py`：标题正则认"## 场面轨"与"## 事件轨"两种，结果多一个 `track_kind`；行号列同时认"段"与"#"；估时来源、渲染、问题文字按实际格式写"场面轨 / 事件轨"；方案差异行"与场面轨的差异"与"与事件轨的差异"都认。地点 / 活动 / 时间 / 锚句 / 估时列名两种格式一致，不改。子空间括号里的"白线、登记桌"按 、，拆开去重（事件轨逐行细分后原来会列出"白线、登记桌、白线"）。表上方的人物清单（"- **Isa**｜…"）不是表行，不会被读成段。
- `templates/script-scene.md`（输入格式参照）：场面轨一节换成 3.7 事件轨（人物清单 + 变化表 + 余韵行规则），注明 3.6 场面轨旧稿同样读。
- `SKILL.md` 输入与交接、`references/stage-1-intake.md` §1.1 / §2.3、`references/duration-rhythm.md` §十一（标题改为"场面轨 / 事件轨"）、README：补一句事件轨，措辞其余不动。

### Added
- `tests/fixtures/scene-track/theorder-ep03-s04-v4.1-events.md`：取自 film-creative `tests/fixtures/review/`（v3.7.0，THE ORDER EP03 场 4 v4.1 采用稿 + 维护者回填事件轨，非盲；第 10、11 行为余韵）。
- `tests/test_scene_track.py` +4（EventTrackTests）：事件轨 11 行、行号 1–11、场地 2 处与子空间去重、时间跳 1 次（第 11 行"跳：几分钟后"）、总估时 69 s、牛棚行的地点 / 活动 / 锚句 / 估时；与 clip01–04 合计 88 s、比值 1.28；方案缺差异行报"与事件轨的差异"，两种差异行写法都放行；去掉"总估时"时按行求和且人物清单不计行；旧"## 场面轨"样本仍读成场面轨。文档接线测试改为断言事件轨。

**已知未处理**：事件轨的"变化"列（余韵行、无台词行）本脚本还不读；这些行在分镜里被扩成整条 clip 的检查另议（见本次汇报的设计选项）。

## 1.5.0 — 2026-09-23

**接 film-creative 3.6 的"场面轨"：参考，不锁定。** 保护区未动（S5 分镜文档、分镜卡模板、校验脚本的哈希不变），改动都在输入契约、S1/S2、时长文档与一个新脚本。**工程完成：测试通过，已提交并打 tag v1.5.0。效果提升未验证**——还没有用新流程排过一场分镜。

### 触发
THE ORDER EP03 场 4 v3 / v4 整场在"Sharks 球场外圈跑道"上跑步，剧本估 57 s，分镜按内容排 74 s，只能靠侧跟拍 / 反向跟移 / 身后跟拍 / 手持斜前轮换补变化；用户看分镜才问"整个场 4 都是球场跑步吗？"。film-creative 3.6.0 起在剧本页正文前加"场面轨"（场地 / 子空间、主要活动、时间跳、每段估时）。本 skill 这一侧：输入契约写着"已确认的台词与场景是锁定内容"，S5 只能改机位不能改场地，估时超出也没有任何信号给用户。用户 2026-09-23 看交接建议后定："我不想锁定，只需要告知即可，包含哪些场地/场景"，改法选"告知＋差异说明"。

### Changed
- `SKILL.md` 输入与交接：场面轨是参考不是锁定——S5 可重分段、换子空间 / 活动 / 地点，不必先问，场级方案写一行"与场面轨的差异：…"（没改写"无"）；分镜合计比剧本估时多 20% 以上时在方案里标提醒，照常继续、不退回，回写剧本页由用户定。"锁定"一条从"台词与场景"改为只锁台词。流水线表 S1 / S5 行、快速路由各一处。
- `references/stage-1-intake.md` §1.1 新增场面轨一行（登记为参考清单）；§2.3 场面轨估时是起点不是上限。
- `references/duration-rhythm.md` 新增 §十一 场级合计与剧本场面轨：读法、可改范围、合计 / 剧本估时 ≥1.2 提醒、5 场 6 个数据点的阈值依据、与 §二 第 7 条的关系（比值高不是压短 clip 的理由；不进 `validate_prompt.py`）。
- `templates/script-scene.md`（输入格式参照）加场面轨一节。

### Added
- `scripts/scene_track.py`：读剧本页"## 场面轨"（无则回退节拍表"总窗口"），列场地 / 场景参考清单（子空间并到场地下）、段、时间跳、剧本估时；`--clips` 汇总各分镜卡"clip 时长 N s"得场级合计与比值，≥1.2 输出提醒（退出码仍 0）；`--plan` 核"与场面轨的差异"一行，剧本有场面轨而方案缺这一行 → 退出码 1。零依赖、不改文件。未入保护区（交接辅助，不是生产核心）。
- `tests/test_scene_track.py`（8 项）+ `tests/fixtures/scene-track/`：EP03 场 4 v4.1 剧本页（film-creative 3.6.0 校准样本，带回填场面轨）与项目实际的 clip01–04、场级方案（2026-09-23 快照）——参考清单 2 处场地 / 5 段 / 1 次时间跳；合计 88 = 22+28+22+16、比值 1.28 提醒；现方案缺差异行报问题；加上差异行后通过；1.03 不提醒；无场面轨旧稿回退总窗口、不要求差异行；缺"clip 时长"退出码 2；文档接线（只锁台词、§十一、SKILL 链接）。

## 1.4.0 — 2026-09-20

变化轨版本（生产 / 导演核心的有意变更；保护区哈希在本提交重设）。**工程完成：测试通过（103 项），已提交并打 tag v1.4.0。效果：非盲呈现改善**——触发案例的 v3 尚未生成对照片，没有盲测（`validation-log.md` 待验证队列 13）。

### 触发
reckless-rivals EP02 场 1 clip01 v2.1（20 s / 7 镜，E 层 `节奏档 | 表演`、`台词轨 | 窗口`、`语速词每秒 | 3`；1.3.2 校验 0 error / 2 warn）→ 成片 `宿敌海报拍摄片段.mp4`。`measure_clip.py --prompt`：切点 2.75 / 6.17 / 7.71 / 10.25 / 13.62 / 17.04 逐镜跟随 Prompt（误差 ≤0.4 s），有声 28%，实测 2.65 词/秒。**成片 = 设计，问题在设计。** 用户判定"画面始终没有变化的感觉"；逐镜抽帧：七镜全固定机位、从镜 3 起全是近景 / 特写、光全程正面暖白、每镜一个同等幅度的微动作、镜长 3/3/2/3/3/3/3 无对比、全片最大物理动作（并肩→转身到鼻尖对鼻尖）放在越监视器的中景里且镜头开始时已转完、冷开场（镜 1）与第一次对眼（镜 5）同构图同状态。

### 定位（根因）
1. `duration-rhythm.md` 与 W22–W29 全是时间维度；画面轨只管切点位置，不管切过去后画面变了没有。Lumet"30 秒内至少一次镜长或运动对比"只是 §四 末行的一句"A 层写明"，不是设计步骤、字段或校验。
2. `节奏档 | 表演` 把 W22–W29 全降为 INFO，由编译者自填——自我豁免；无台词段正是视觉单调风险最高处。
3. 硬规则 8（机器只在人物动时动）、硬规则 15（一镜一事件 + 一记忆点）、G6"同构图重复"各自正确，合力是"等长固定机位微动作序列"；没有规则要求 clip 内部存在对比。
4. 语速默认偏慢：能力表 §7 基数 2.5 词/s、`duration-rhythm.md` 常速 3.3–3.5 是区间；实测 D10a ≥3.6、D10b ≥4.1 可用，说明默认值让窗口偏宽、模型往里塞停顿。

### Added
- **S5 变化轨**（`stage-5-directing-storyboard.md` §5.1f；`director-grammar.md` §4.1 + 推导规则第 11 条；`duration-rhythm.md` §二 改为三条轨、§四、§六、§七）：每条 clip 先声明峰值事件 → E 层 `峰值镜 | N`，峰值镜拿最可见景别 + 本 clip 首要的有动机运镜；每镜声明四维变化 景别 / 机位或运镜 / 光 / 动作幅度（0–3）。设计下限：镜长有对比（最长 ≥2× 最短）、至少两个维度各变一次、峰值不落在全景或越肩、无台词段同样受约束。幅度刻度 0 静止 / 1 部位级 / 2 头手上身一个动作 / 3 全身位移。
- **C 层可解析字段**（`templates/shot-card.md`、`templates/prompt-templates.md` T1 与英文骨架、`stage-6` §6.2 / §6.5）：`【变化：景别 全→近｜机位 固定→推｜光 正→侧｜幅度 1→3｜新增 X】` / `[CHANGE: size W→CU | camera static→push | light front→side | amp 1→3]`，写在景别标注之后同一行（行首【】是段落边界）；首镜写基线，`=` 不变，重复构图第二次填 `新增`。E 层 `峰值镜 | N`、`无声段理由`。
- **`scripts/variation_checks.py`** + `validate_prompt.py` 接线：W30 连续三镜四维零变化 / 全 clip 变化维度 <2 / G6 同构图第二次未声明新增；W31 镜数 ≥4 且最长 <2× 最短；W32 未声明峰值镜 / 镜头缺变化字段（缺字段时按标注推断景别 / 机位两维并标"推断"）；W33 峰值镜是全景 / 远景、越肩、或固定机位而运镜给了别的镜。单镜 clip 只出 INFO。JSON 输出加 `variation`。
- **档位 = 检查集**（`rhythm_checks.py`）：对话档 = W22–W29 + W30–W33；表演档 = W24（无声段功能：每段无声一个功能词写 E 层 `无声段理由`，≥2 镜时查）+ W30–W33；W22 / W23 / W25–W29 在表演档不查。没有任何档位把全部检查降为 INFO；INFO 行标明本 clip 用的检查集。
- **`measure_clip.py` 视觉变化谱**：每镜抽一帧（按 Prompt 声明切点，无 Prompt 按检出切点）存 jpg + 拼图（`--frames DIR`，默认 `<视频>_frames/`，`--no-frames` 跳过）；每镜 亮度 / 边缘密度 / 主体占比代理（中心色调占比）/ 镜内运动（首末帧像素差）；相邻镜 Δ直方图、Δ边缘、Δ主体 → 该切点"变 / 平"；clip 级 镜长对比、边缘密度极差、主体占比极差、有镜内运动的镜；与 Prompt 变化轨并列给出每个切点的判定 变化已执行 / 平在设计 / 平在渲染 / 渲染多出变化。只用 ffmpeg + 标准库，不引入 numpy / PIL。阈值（Δ直方图 0.12、镜内运动 0.08）是启发式，待更多样本核（待验证 14）。
- `tests/test_variation.py`（14 项）：字段语法、推断、W30–W33 各触发条件、G6 仅在 L19 / 暧昧下报、单镜跳过、表演档不能压掉变化轨、语速默认 3.5 回归、触发案例形态回归（≥2 项 W30/W31/W32）、合成视频的视觉变化谱与拼图。`test_rhythm.py` 加检查集测试。
- 硬规则 19（SKILL.md）；快速路由加"画面始终没有变化的感觉"；`stage-7` 诊断表与 S5 §5.9 各加一行；`validation-log.md` 记本案例 + 待验证 13 / 14 + 两条标签变更。

### Changed
- **语速默认 3.5 词/s**：`validate_prompt.speech_parameters` 与 `measure_clip.declared`（现读 E 层）基数 2.5 → 3.5；`duration-rhythm.md` §二 第 1 条改为"常速交锋 3.5；短促可到 4；慢的戏 2.5–3 并在说法里写明"（常速不再是区间）；能力表 §7.2 / §7.3 预算表按 3.5 重排，2.5 保留为美式英语均值 `[一手]` 并注明是下限；SKILL 硬规则 17；templates E 层示例值。
- SKILL 硬规则 8 / 15 各加一句与变化轨的关系（原规则不删）；硬规则 12 列 W30–W33 与检查集；流水线表 S5 产出加变化轨、S7 加视觉变化谱。
- `genre-packs.md` G6"同一构图重复第二次多一个眼神"改为"重复第二次必须多一样东西，且写进变化字段的「新增」"。
- 示例 01 / 05 / 06 加变化字段、峰值镜、无声段理由并保持 0 warning；01 镜长改 4/5/3/8、06 改 6/3/4/4/3 以满足 W31（台词窗口不变）。示例 04 与 `examples/production/30s-fight-t2v` 保持 2.3.1 原文（生产记录 sha 锁定），它们现在报 W32 是预期的缺声明示范。
- README 版本与文件地图；`tests/test_protected_zone.py` 基线重设，新增 `variation_checks.py` / `rhythm_checks.py` 入保护区。

### 回归结果（触发案例 v2.1 Prompt，文件原样）
- 1.3.2：`0 error / 2 warn`（W05 摄影师 9 词 / 3 s 窗口 @3 词/s = 3.0 s；W05 单元 4）。
- 1.4.0：`0 error / 6 warn`——**W30**（镜头5 与镜头1 同构图但未声明新增元素，G6）、**W31**（镜长 3/3/2/3/3/3/3 无对比：最长 3 < 2× 最短 2）、**W32 ×2**（未声明峰值镜；7 镜缺变化字段）、W05 ×2（E 层仍显式写 `语速词每秒 | 3`）。满足"至少报出 W30/W31/W32 中两项"。INFO：`节奏档 表演 … 检查集 W24（无声段功能）+ W30–W33`（E 层已有 `无声段理由`，故 W24 不报）；`变化轨：镜2 景别✓机位✓(推断) · 镜3 景别—机位✓ · 镜4 景别✓机位— · 镜5 景别✓机位— · 镜6 景别✓机位— · 镜7 景别—机位—；维度变化次数 景别 4 / 机位 2 / 光 0 / 幅度 0；峰值镜 未声明`。
- 语速回归：摄影师句实际 9 词（任务书写 8 词）。文件原样（显式 3 词/s）仍报 W05；删掉 `语速词每秒` 行改用新默认 3.5 → 估时 2.57 s，W05 不报；W22 建议窗口 ceil₀.₅(2.57 ÷ 0.9) = 3.0 s，实际 3 s，松弛 0 → W22 不报。
- 视觉变化谱（成片 vs v2.1 Prompt，`--frames`）：切点 Δ直方图 0.33 / 0.44 / 0.25 / 0.27 / 0.28 / 0.17 全部"变"（每一次切都换了脸或景别，渲染没有偷懒）；镜内运动 0.232 / 0.117 / 0.192 / 0.215 / 0.167 / **0.061 静** / **0.040 静**（镜 6、7 两个越肩近景基本不动）；clip 级：镜长对比 3.00/2.00 = 1.5×（<2×）、边缘密度极差 0.144、主体占比极差 42%。变化轨对照：镜 1→2 … 5→6 变化已执行，镜 6→7 声明零变化（推断）而成片有变（渲染多出变化：两张不同的脸）；结论"平在设计 无；平在渲染 无；峰值镜 未声明"——即成片逐切点都换了，平的是设计层面：没有峰值、没有镜长对比、没有光与幅度的变化。拼图 `宿敌海报拍摄片段-sheet.jpg` 目视核对：七帧里五帧是相距十厘米的两张脸。
- 注：任务进行中该项目的 v2.1 文件已被另一会话改为 v3（15 s / 6 镜，已带 `峰值镜 | 1` 与变化字段，1.4.0 校验 0 error / 3 warn：W24 ×2 结尾 7 s 无台词、W27）；上述 v2.1 回归用的是任务开始时读取的原文副本。

### 说明（与任务书设计的差异）
- G6 的"同构图第二次未声明新增"归入 W30（同一族：零变化），非 G6 场景只出 INFO"重复构图"。
- W33 多加一个触发："峰值镜固定机位而 clip 的运镜给了别的镜"——直接对应本案例（缓推给了并肩的镜 2，转身在固定的镜 4）。
- 表演档的 W24 只在 ≥2 镜时查，单镜 clip 的无声就是表演本身；单镜 clip 不查 W30–W33（镜内变化由表演节拍承担）。
- 缺变化字段时脚本按标注推断景别 / 机位两维并标"推断"，而不是直接放弃 W30，这样旧 Prompt 也能报出连续零变化。

## 1.3.2 — 2026-09-19

实测更新（文档与验证账本，无代码变更）。用户手改的 D10a/b/c 成片量化：切点逐镜跟随（误差 ≤0.5 s）；50 词装 15 s 窗口 → ≥3.6 词/秒，14 词句放 3 s 镜 → ≥4.1 词/秒，用户判定可用。**语速是窗口的函数，不是模型常数**：`duration-rhythm.md` §一、§二据此改写——`语速词每秒` 是设计参数（常速 3.3–3.5、短促可到 4、慢戏 2.5–3 并写说法），上限由可懂度判定。`validation-log.md` 记三条；W29 容差是否放到 1.5 s 待再测。

## 1.3.1 — 2026-09-19

把 1.3.0 的"连续台词轨"从可选字段变成模型本身（自查：1.3.0 文档前后打架、连续轨靠 E 层字段记得写，属补丁痕迹）。

### Changed
- `duration-rhythm.md` §二、§三 重写为两条轨：台词轨（校准语速、整条一次取整、句句相接）与画面轨（切在信息上、整数秒、≥2 s、能并进台词镜的动作不单独成镜），再对齐（窗口 = 起点 + 所属镜，可溢出 ≤1 s）；D10a 例：15.2 s 轨 + 4 s 无声 = 20 s。§九 补连续轨下的 D05 例。
- 对话场（台词净时长 ≥25%）**默认 台词轨 = 连续**，无需 E 层字段；`台词轨 | 窗口` 才退回逐窗口 W05（表演档默认窗口）。`validate_prompt.py` 与 `rhythm_checks.py` 同步。
- `stage-5` §5.2 第 7 步、`stage-6` 台词规则改为两轨表述。
- 测试：example-04（对话场）的"长句保留时间审阅"改为接受 W05 / W29 / 台词轨 INFO；保护区哈希重设。

## 1.3.0 — 2026-09-19

连续台词轨与语速校准。触发：用户把 1.2.0 规则产出的 D10a/b/c（25 / 19 / 18 s）手改到 21 / 15 / 14 s 后出片可用——说明 1.2.0 的时长模型比模型实际说话慢一档：逐句整数取整（7 句吃掉 3 s）、逐窗口 W05 逼人撑窗口、语速 3 词/秒低于实测 3.36。**工程完成：测试通过。效果提升：用户反馈手改版出片可用（未量成片）。**

### Added
- E 层 `台词轨 | 连续`：台词按连续轨排，窗口只标起点与所属镜，一句可溢出到下一镜 ≤1 s（`台词轨溢出容差`）；`rhythm_checks.track_schedule` 链式检查 → **W29**（压过下一句起点 >1 s 或超过片长才报），取代逐窗口 / 逐镜的 W05（总占比 W05 保留）。用户手改 D10a 在此模式 @3.3 词/秒下从 10 个 WARN 降到 2 个（仅开头 4 s 无台词的 W24）。
- `measure_clip.py --prompt` 输出校准行：词数 / 有声秒 = 实测词/秒（下限），回填 E 层 `语速词每秒`；D05 成片 3.36 词/秒。
- `duration-rhythm.md` §十 校准流程；§二 改为台词轨一次取整；§三 连续轨行；§一 校准依据行。
- `tests/test_rhythm.py` +2（连续轨溢出 / 堆积 / 片长；轨模式与轨和推导）。

### Changed
- 时长推导参考 = ceil(净时长 ÷ 填充率 + 无台词镜 + 动作节拍)，不再逐句取整求和（逐句合计只作参考显示）。
- `SKILL.md` 硬规则 18、`stage-6` 台词规则、`prompt-templates.md` E 层字段说明。

## 1.2.0 — 2026-09-19

时长与节奏版本（生产 / 导演核心的有意变更；保护区哈希在本提交重设）。触发：sable-point-college EP01 D05–D07 被用户反复批评"时间设计和利用不充分、节奏慢"，且 2.5 成片实测确认节奏来自 Prompt 的时间设计。**工程完成：测试通过。效果提升未验证**——尚未用收紧后的 Prompt 重新生成并对照。

### 调研
- 官方：《Seedance 2.5 提示词指南》在线版（2026-09-18）时间戳段与 30 秒范例逐镜秒数（3/3/4/4/4/4/3/3/2，8/9 镜有台词）。
- 实测：EP01 D05 成片切点与 Prompt 逐一对应（误差 ≤0.3 s），有声占比 59%，窗口内静音空档 22 段 → 记入 `validation-log.md`。
- 剪辑与镜长：Murch（眨眼即剪、六准则）、Lumet（节奏变化 / 过长）、Bordwell 2007（对话场 ASL 2–4 s）、Cutting et al.（当代 ASL 4.3 s；特写更短）、竖屏短剧实测 2.5 s（第三方搜索摘要，原页下架）。

### Added
- `references/duration-rhythm.md`：§九 一句不等于一镜（视觉单元分镜法：拆分句 → 每分句一个视觉事件 → 一句可跨两镜、尾段画外 → 相邻镜至少换一项）；clip 时长从内容推导（不默认 30 s）、台词窗口 = 估时 ÷ 0.9 向上取 0.5 s（写进 Prompt 取整数秒、松弛 ≤1 s）、镜长分布目标（对话 2–4 / 蒙太奇 2–3 / 持续 5–8 需理由）、无声段功能表与 ≤20% 阈值、Prompt 提速写法七条、检查项对照。
- `scripts/rhythm_checks.py` + `validate_prompt.py` 接线：W22 窗口过宽、W23 长镜（>5 s 无台词无运镜；>8 s 任何镜）、W24 无声段（窗口外 >20%、首尾 >2 s）、W25 对话场平均镜长 >4.5 s、W26 声明时长比推导参考多 ≥4 s、W27 减速词、W28 固定机位整句一镜 ≥4 s；INFO 节奏档 / 平均镜长与分布 / 台词净时长·建议窗口·松弛 / 推导参考。对话场判定：台词净时长 ≥25% clip，E 层 `节奏档` 可覆盖；其余阈值均可在 E 层覆盖（`动作节拍秒数` 登记与台词同镜的实体动作）。
- `scripts/measure_clip.py`：用 ffmpeg 量成片的切点、平均镜长、有声占比、首尾与最长静音，`--prompt` 并列 Prompt 声明。
- `tests/test_rhythm.py`（7 项）。
- `prompt_structure.dialogue_checks(..., fill=0.9)`：W05 的 90% 阈值改为读 E 层 `台词填充率`（与 W22 的建议窗口同一旋钮）；项目写 `台词填充率 | 1.0` 即"窗口 = 词数 ÷ 语速向上取整秒"的口径，只在台词放不下时报 W05。

### Changed
- `SKILL.md`：硬规则 18（对话场一句不等于一镜、时长从内容推导）；S5 读 `duration-rhythm.md`；S7 有成片时用 `measure_clip.py`；硬规则 12 提及 W22–W27；快速路由加"成片节奏慢"。
- `stage-5` §5.2 第 5 步切点跟信息走（§九）、第 7 步 clip 总长从内容推导；`stage-6` 一句分两段引号跨镜的写法；`prompt-templates.md` E 层节奏字段说明；`stage-6` §6.2 台词窗口规则与【贯穿要求】节奏指令；`prompt-templates.md` T2 贯穿要求加节奏指令占位；`stage-7` 诊断表加"节奏慢 / 拖"一行；能力表 §4.3 加官方范例逐镜秒数与实测注记；`validation-log.md` 首条记录。

## 1.1.0 — 2026-09-17

导演语法与官方对齐版本（生产 / 导演核心的有意变更；保护区哈希在本提交公开重设，见 `tests/test_protected_zone.py` 与 `tests/acceptance-1.1.0/protocol.md`）。**工程完成：测试通过；效果提升未验证**——没有生成对照成片，也没有用户盲选。

### 调研（`tests/acceptance-1.1.0/`）
- 官方：火山方舟《Seedance 2.5 提示词指南》PDF 通读 41 页；在线教程、《创建视频生成任务 API》、《2.0 系列提示词指南》FAQ、Seed 博客复核，补齐 PDF 之外的参数与限制。
- 导演语法：Mackendrick、Mamet、Hitchcock/Truffaut、Lumet、Murch、Bordwell、Block、Katz、Proferes、Weston、Deakins、Fincher、Lubezki、Villeneuve、Spinotti/Mann、Richards/Zhao、Gerwig/Levy、Nolan 及 Every Frame a Painting 等；每条标注已读原页 / 搜索摘要。
- 台词速率：NCVS、Tauroza & Allison 1990、Yuan/Liberman/Cieri 2006、Netflix TTSG、Banse & Scherer 1996、中文配音行业数值；官方无每秒字数规则。
- 暧昧 / 爱情：Sciamma（BFI）、Lachman（Film Stage）已读原文；Wong/Doyle、Linklater、Guadagnino/Mukdeeprom、Wright、Coppola、Jenkins、Haigh、Lubitsch/Wilder 经搜索摘要。

### Added
- `references/director-grammar.md`：三个元问题、景别、运镜（动机 → 动作 → 终点）、动线与调度（走位表、平面 / 纵深、动线语法、轴线与合法越轴、三种对话图形）、覆盖与剪辑（Murch 六准则、眨眼即剪、POV 三联、迟进早出）、构图与镜头（对比 / 亲和、焦距即心理、机高）、光与色、决策载体、类型语法（悬念 / 视觉喜剧 / 动作 / 封闭空间 / 暧昧爱情）、十条推导规则、来源与核实状态。
- S5 §5.0 决策载体（文字 / 关键帧 / 白模 / 动作视频 / 宫格）、§5.1c 走位表、§5.1b 景别序列；S5b §5b.0 从载体推资产；T3b 白模模板；【严格排除】可选段位（官方范例）。
- 能力表 §7 台词每秒预算（英文 2.5 词/s、中文 4 字/s、占比 2/3 的一手依据与预算表）。
- `genre-packs.md` G5 视觉喜剧、G6 暧昧 / 爱情（难题 → 机制 → 选择 → 风险）；`director-lenses.md` L19 凝视与克制，每条透镜补"作用机制"与"语法依据"。
- 示例 05〈楼梯间的信〉（信息差、有动机的推近与反向跟拍、关键帧载体）、示例 06〈阳台的烟〉（错位注视、身体先于头脑、无 bgm），均通过校验。
- `tests/test_directing_release.py`：W04 阈值对齐官方范例、W19 退役、示例 05 / 06 清洁、语法与载体接线、无固定意图映射、词典不再规定运镜、台词预算有一手来源。
- 硬规则 15（每镜负载）、16（决策载体）、17（台词预算）。

### Changed
- `seedance-2.5-capabilities.md`：按官方在线文档重写——Model ID、duration [4, 30] / -1、resolution 480p / 720p / 1080p（无 4k）、ratio 取值与锁定任务只允许 adaptive、输入规格、真人人脸限制、仅音频输入、11 种语言、mov 编码、水印、omni_reference_task_type、不支持的参数、提示词长度建议、声音符号规范、分段绑定参考的官方写法、2.0 FAQ、官方 `sd25-pe` Skill 与模板文档的存在；分辨率与发布时间由 `[第三方]` 改为 `[官方]`。
- `validate_prompt.py`：W04 改为 <2 s 与 >10 镜 / 30 s 的审阅提示（官方 30 秒范例 9 镜、最短 2 s）；W19 标点推意图退役。
- `stage-4-performance.md`：删除"每个动词配一个身体证据（恳求 = 前倾 + 手伸出）"的固定映射与"反讽 = 台词与动作相反"的规则化；人物语言开发交回创意侧，S4 只取执行需要的语音信息；多人同框由"默认拆单人"改为风险登记与可协商方案；台词长度按预算；官方符号规范可选。
- `stage-5-directing-storyboard.md`：重写。5.4 推近的动机由"靠近情绪"改为"人物内部变化"（消除与 W15 的自相矛盾）；5.2 加入"机器要不要动""每镜负载"；5.6 复合运镜写同步 / 先后；5.9 诊断表改为按顺序排查而非单因单解；5.1 允许有意的时空省略；5.1b 无主控句时用观众问题替代。
- `stage-6-prompt-compiler.md` / `prompt-templates.md`：每镜负载规则；镜头段首标注 ≤40 字、运镜动机展开写在标注后；台词不为每句新编听者反应；T8 延长不再复述原视频末态，直接指代、可换景别与速度、可引用其他视频；T3b 白模。
- `stage-7-qa-continuity.md`：加导演语法检查行；口型 / 动作被赶 / 机器乱动 / 长镜头崩的诊断改为多因排查。
- `camera-vocabulary.md`：按官方"景别 + 机位 + 运镜 + 速度 + 质感"五要素重组；加焦距与景深写法、机高、运镜终点示例、反向跟拍、急推变焦、匹配转场、光的动机、声音符号。
- `externalization-lexicon.md`：最后一列由"摄影 / 声音"改为"声音 / 可见性条件"，去掉情绪 → 运镜的固定伴随。
- `templates/shot-card.md`：加观众问题、轴线、决策载体、焦距感、运镜动机 → 终点、每镜主要事件与记忆点；`templates/script-scene.md`：策略列与距离曲线改为按需。
- `SKILL.md`：S5 读取项加 grammar；硬规则 8 加动机与终点；硬规则 10 指明导演逻辑的存放处；工作边界补"不开发人物语言"；运行模式表的"触发词"改为"典型说法（示例）"；类型叠加加视觉喜剧 / 暧昧爱情；示例列表加 05 / 06。
- `source-analysis.md`、`validation-log.md`：登记新来源与标签变更；待验证队列加 9–12 项。

### Retired
- W19（标点推意图）；"抗拒快切"作为 W04 的依据；`stage-4` 角色声音表；"两人对视不动 → 回 S3""长 clip 后半崩 → 切成两个 15 s""口型对不上 → 换中文 / 英文"的单因单解。

### 未改动（保护区哈希保持 1.0.0）
- `assets/emotion-library.json`、`references/emotion-performance.md`、`references/emotion-index.json`、`references/performance-record.md`、`references/production-workflow.md`、`templates/performance-record.json`、`templates/production-record.json`、`templates/reference-asset-brief.md`、`templates/asset-registry.md`、`scripts/prompt_structure.py`、`scripts/production_contract.py`、`scripts/production_preflight.py`、`scripts/emotion_library.py`、`scripts/performance_checks.py`。

## 1.0.0 — 2026-09-07

独立的 Seedance 2.5 生产后端 Skill，内容取自 [Film-Seedance-Director](https://github.com/Anelse0/Film-Seedance-Director) **v2.3.1**（用户指定以 2.3.1 为生产基线；创意前端由 film-creative Skill 承担，其基线为 2.6.0-alpha.3）。

- 范围：S1 资源读取、S2 任务识别、S4 表演外化（含情绪表演库）、S5 导演与分镜、S5b 参考资产、S6 Prompt 编译、S7 检查；表演优先路由（performance / production / raw）完整保留；生产契约与 30 秒设计沿用 2.3.1。
- 移除创意侧文件：stage-3a/3b/3c、concept-generation、screenwriting-traditions、validate_concept、story 模板、概念示例与概念测试。
- 新增「输入与交接」契约：接受任何来源的剧本场景文本；剧本层缺口只登记不代写；已确认台词与场景为锁定输入。
- 共用文件（stage-1-intake、scene-parameters、causal-chain、anti-mechanical、genre-packs、stage-4、stage-5、director-lenses、script-scene 模板）中指向创意侧文件或概念模式的引用改为交接说明；其余生产核心文件与 v2.3.1 逐字节一致。
- `tests/test_protected_zone.py` 以 1.0.0 为基线哈希锁定生产/表演核心；对照 v2.3.1 的差异可在源仓库 tags 中核查。
- 历史版本记录（1.2.0–2.3.1 及其后的 2.4.0–2.6.0 开发线）见源仓库 Film-Seedance-Director 的 CHANGELOG 与 tags。
