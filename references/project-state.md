# 已保存项目的版本、依赖与恢复

仅在 execution-contract.md 允许保存时使用。未保存探索留在对话里，不创建目录。已有项目按需升级，不要求回填历史记录。

## 最小索引

故事目录的 `project-state.json` 以 `templates/project-state.json` 起步；小项目也可把等价字段放在 00_brief.md，自动检查时才使用 JSON。记录目标、排除项、保存约定、硬锁、当前产物路径与哈希、确认版本、历史快照、依赖和未决事项。索引不复制正文，路径均相对索引。恢复时读索引及相关原文，摘要不替代锁定台词与表演。

一个稳定 artifact ID 对应一种成果（如 script-scene-01）；版本号和磁盘路径可以改变，ID 不变。`current` 是当前采用稿，`approved` 是有用户证据的确认快照，`drafts` 是未采用尝试。三者不自动互相替换。用户新的改写授权在范围内生效，其他硬锁不变。

## 保存新版本

1. 读取 current 并核对哈希；磁盘已变时先理解实际改动，不覆盖未读版本。
2. 修改前复制旧正文到 `history/<artifact-id>/vNN.md`，记录路径与哈希；不只改文件头版本号。确认稿也指向不可变快照，不能只绑定不断更新的 current 路径。
3. 将新稿完整写到临时同目录文件，核对后替换当前稿；更新 current 路径、版本、哈希与实际依赖。尝试稿放 drafts，只有采用时才切 current。
4. 当前稿与依赖变更不会自动更新 approved、风险接受或生产审阅。下游按依赖实际标记需复核，不能只刷新哈希以绕过审阅；只重做授权且受影响的部分。
5. 最后写索引。若中断，检查器会发现哈希/路径不一致；先恢复旧稿或补记已完成的新稿，再继续，不自动宣称一致。

`python3 scripts/project_check.py project-state.json` 为只读检查：当前/历史/确认文件可读且哈希相符；依赖版本改变或间接依赖失效时报告 stale。只计算依赖，不评判创作质量，也不替代现有严格生产预检。未记录的旧项目状态是 unknown，不伪造历史确认。

## 正典、素材与生产记录

- IP 的 ip.md 仅维护共享正典及其来源。本集暂定发现留在本集故事/人物段；用户明确建立共享设定或已有正典维护授权时再合并，保留旧值来源，不顺手改变其他故事。
- assets/assets.md 使用稳定 ID（如 char-a-front-v1）。上传顺序只在本条 clip 的记录映射 img1/vid1/aud1；已有生产记录 schema 与 Prompt 引用不变。换序只影响该条上传映射，换素材内容才影响依赖它的其他条目。
- `06_prompts/scene-XX-clipYY.production.json` 与对应 Prompt 并列；路径与哈希按既有 production-workflow.md。检查器不写入生产记录，也不代替其审阅。
- 全剧本阅读视图是按场序汇集的派生物，标明源版本；修改回各场原文，禁止另维护一套竞争正稿。
